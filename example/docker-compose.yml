# still creating compose build
version: '3'
services:
  ifirlana:
    build: .
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 256M